name: 'AWS SSM Parameters to Env Variables'
description: 'Load AWS SSM Parameters into the session environment variables'
author: 'Haus of Web'

branding:
  icon: cloud
  color: orange

inputs:
  ssm-path:
    description: 'AWS SSM path for parameter (eg. `/ssm/parameter`)'
    required: true
  get-children:
    description:
      'Define to get parameters by path, retrieving all children values'
    required: false
  prefix:
    description: 'Set a prefix on the environment variable'
    required: false
  decryption:
    description: 'Whether the parameter must be decrypted or not'
    required: false
  mask-values:
    description:
      'Indicates if extracted values should be masked in GitHub Action logs'
    required: false

runs:
  using: node24
  main: dist/index.js
